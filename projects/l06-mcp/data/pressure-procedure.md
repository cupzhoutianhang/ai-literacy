# Pressure-drop procedure

Read the latest `pressure_in` and `pressure_out` values for the same asset.
Subtract outlet from inlet.  A result above 1.2 bar is a teaching alert and
must be verified by a person before any operational action.
